packages:
  - apps/*
  - packages/*

# pnpm 10 ignores dependency build scripts until they are listed here. Vitest and
# Vite need esbuild's platform binary, so its install script is allowed to run.
onlyBuiltDependencies:
  - esbuild
