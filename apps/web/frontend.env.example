# Copy this file to apps/web/frontend.env before starting the frontend.
# The backend Compose stack must be running first so its external network exists.

VITE_API_MODE=http
VITE_API_BASE=
API_UPSTREAM=api:3000
BACKEND_NETWORK=module-atelier_backend
WEB_BIND_ADDRESS=127.0.0.1
WEB_PORT=8080
